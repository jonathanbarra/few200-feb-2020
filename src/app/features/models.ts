export interface ShoppingItemModel {
  id: string;
  description: string;
  purchased: boolean;
  isTemporary: boolean;

}

export interface BookItemModel {
  title: string;
  author: string;
  format: string;

}
